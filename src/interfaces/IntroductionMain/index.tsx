export interface BoxMusic {
  id: number;
  img: string;
  subtitle: string;
  title: string;
}

export interface MakeTheMostFeatures {
  id: string;
  name: string | React.ReactNode;
  desc: string | React.ReactNode;
  link: string;
  linkName: string | React.ReactNode;
}
