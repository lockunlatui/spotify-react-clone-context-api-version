export enum Apis {
  ApiV1 = "/api/v1",
}

export enum Routes {
  HomePage = "/",
  HomePageHasAuthenticate = "/home",
  Open = "/open",
}

export enum ApiRoutes {
  Logout = Apis.ApiV1 + "/logout",
}
